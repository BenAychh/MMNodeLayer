var chai = require('chai');
var chaiHttp = require('chai-http');
var server = require('../app');

var should = chai.should();

chai.use(chaiHttp);

describe('get a user\'s potential matches', () => {

});

describe('insert a potential match', () => {
  
});

describe('deactivating a user', () => {
  
});

describe('activating a user', () => {
  
});

describe('get a user\'s matches', () => {
  
});

describe('get a user\'s list of people they are interested in', () => {
  
});

describe('can add to the list of people a user is interested in', () => {
  
});

describe('can remove from the list of people a user is interested in', () => {
  
});